<Window x:Class="Assignment2.App.Views.AnimalEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Animal Editor"
        Height="330"
        Width="650">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <Button Content="Customers" Margin="0,0,0,5"/>
            <Button Content="Animals" Margin="0,0,0,5"/>
            <Button Content="Exit"/>
        </StackPanel>

        <Grid Grid.Column="1" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:"/>
            <TextBox Grid.Row="0" Grid.Column="1"
                     Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Type:"/>
            <TextBox Grid.Row="1" Grid.Column="1"
                     Text="{Binding Type, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Breed:"/>
            <TextBox Grid.Row="2" Grid.Column="1"
                     Text="{Binding Breed, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Sex:"/>
            <TextBox Grid.Row="3" Grid.Column="1"
                     Text="{Binding Sex, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Owner:"/>
            <ComboBox Grid.Row="4" Grid.Column="1"
                      ItemsSource="{Binding Customers}"
                      SelectedItem="{Binding SelectedOwner}"
                      DisplayMemberPath="Surname"/>

            <StackPanel Grid.Row="5"
                        Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Margin="0,10,0,0">
                <Button Content="Cancel"
                        Width="80"
                        Margin="0,0,5,0"
                        Command="{Binding CancelCommand}"/>

                <Button Content="Save"
                        Width="80"
                        Command="{Binding SaveCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>