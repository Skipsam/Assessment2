<Window x:Class="Assignment2.App.CustomerEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="We-Treat-It: Add Customer" Height="300" Width="400" ResizeMode="NoResize">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="HorizontalAlignment" Value="Right" />
                <Setter Property="Margin" Value="4" />
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Width" Value="250" />
                <Setter Property="Margin" Value="4" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="First Name:" />
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Surname:" />
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Phone Number:" />
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Address:" />

        <TextBox Grid.Row="0" Grid.Column="1" Name="firstName" />
        <TextBox Grid.Row="1" Grid.Column="1" Name="surname" />
        <TextBox Grid.Row="2" Grid.Column="1" Name="phoneNumber" />
        <TextBox Grid.Row="3" Grid.Column="1" Name="address" Height="100" TextWrapping="Wrap" AcceptsReturn="True" />

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Width" Value="50" />
                    <Setter Property="Margin" Value="4" />
                </Style>
            </StackPanel.Resources>
            <Button Content="Cancel" IsCancel="True" Click="OnCancel" />
            <Button Content="Save" Click="OnSave" />
        </StackPanel>
    </Grid>
</Window>